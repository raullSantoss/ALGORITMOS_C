#include <stdio.h>

int main()
{
    int numero =0;
    printf("digite um numero inteiro");
    scanf("%d", &numero);

    if (numero % 2 == 0)
    {
        printf("numero e par: %d", numero);
    }
    else 
    {
        printf("numero e par: %d", numero);
    }
    
    return 0;
}
